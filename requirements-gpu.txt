# GPU 服务器专用依赖(192.168.10.63)
# 跟 requirements.txt 一起装: pip install -r requirements.txt -r requirements-gpu.txt
#
# funasr 锁 1.1.x(1.3.x API 拆了不可用,详见 docs/部署/踩坑记录.md §10)
# pyannote-audio 3.3.2 + HF 1.20.1 兼容(use_auth_token 已弃用,详见 §8)

# ===== ASR / 说话人(GPU 推理) =====
funasr>=1.1.0,<1.2
modelscope>=1.10
torch>=2.0,<3
torchaudio>=2.0,<3

# ===== pyannote(说话人分离) =====
pyannote-audio>=3.3,<4
pyannote-core>=6.0,<7
huggingface-hub>=1.20,<2

# ===== Whisper(可选,M4A/MP3 fallback) =====
faster-whisper>=1.0,<2
ctranslate2>=3.0

# ===== 音频处理 =====
pydub>=0.25
soundfile>=0.12
librosa>=0.10
