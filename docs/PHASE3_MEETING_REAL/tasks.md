# 任务列表 — PHASE3_MEETING_REAL

最后更新: 2026-06-21
session_id: meeting:PHASE3_MEETING_REAL:tasks
总计: 23 条 (MVP 必做=19, P2=1, 并行/合规=3)

---

## 0. 关键路径速览

```
T-001 基础设施 ─┬─► T-002 限流 ─┐
                ├─► T-003 审计 ─┤
                ├─► T-004 SSO OIDC ─► T-005 SAML
                │                  └► T-006 RBAC ─┐
                ├─► T-007 yjs 集成 ─► T-008 WS 服务 ─► T-009 快照 ─┐
                │                                                     │
                └─► T-010 Event Bus ─► T-011 飞书 Adapter ─┐         │
                                  ├─► T-012 钉钉 Adapter ─┤         │
                                  ├─► T-013 企微 Adapter ─┤         │
                                  └─► T-014 重试死信 ────┤         │
                                                            │         │
                                            ┌───────────────┘         │
                                            ▼                         │
                                  T-015 Excel 导出 ─► T-016 字段映射    │
                                                                          │
                                            ┌───────────────────────────┘
                                            ▼
                                  T-017 妙记回调 ─► T-018 写入协同文档
                                                          │
                                                          ▼
                                                    T-019 E2E 联调
                                                          │
                              ┌───────────────────────────┼──────────────────┐
                              ▼                           ▼                  ▼
                        T-021 说话人校准            T-022 私有化镜像    T-023 P2 历史回溯

并行/合规: T-020 微信开放平台申请(审核 7-15 天,与其他并行启动)
```

---

## 1. MVP 主线 (T-001 ~ T-019)

### T-001 基础设施确认 (PG / Redis / S3)
- **负责人**: 待分配 (SRE)
- **工期**: 1 天
- **依赖**: 无
- **状态**: pending
- **验收标准**:
  - PG 集群可用,JSONB 扩展启用,目标库 `vpbuddy` 创建
  - Redis 单实例可达,MVP 用 Redis Streams 作 Event Bus
  - S3 bucket `vpbuddy-exports` 创建,生命周期策略 7 天清理
  - `.env.example` 收录所有连接串,本地 `docker compose up` 可拉起

### T-002 API Gateway 限流 (REQ-302783)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-001
- **状态**: pending
- **验收标准**:
  - Redis token bucket 实现,key 形如 `ratelimit:{tenant_id}`,100 QPS/租户
  - 网关层 middleware,触发 429 时返回 `Retry-After` 头
  - 单测覆盖:正常通过 / 超限拒绝 / 桶恢复 / 多租户隔离
  - 压测报告:单实例 1k QPS 下 P99 < 5ms

### T-003 API Gateway 审计日志 (REQ-B82FAC)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-001
- **状态**: pending
- **验收标准**:
  - 记录 4 元组: `tenant_id / user_id / method+path / status_code`
  - PG 表 `audit_logs`,默认留存 90 天(写 cron 清理)
  - P2 标记: `retention_days` 字段预留,本期先写死 90
  - 不记录请求体(避免敏感信息泄露),仅元数据

### T-004 SSO OIDC 集成 (FEAT-1763F9, QUE-661567, RISK-33E783)
- **负责人**: 待分配 (后端 + 安全)
- **工期**: 3 天
- **依赖**: T-001
- **状态**: pending
- **验收标准**:
  - 主路径 Azure AD 跑通:`GET /oauth/azure/authorize` → 回调 → 签发内部 JWT
  - IdP 抽象层 `IdpProvider` 接口,后续可加 Okta/Auth0/飞书钉钉无需改业务
  - Redis 缓存 IdP 公钥(JWKS),TTL 1h
  - **RISK-33E783 应对**:对 IdP 端点调用加本地缓存(用户信息)+ 429 响应指数退避(复用 T-014 参数,上限 60s,3 次后切本地缓存兜底)
  - 待 V 拍板:Azure AD 确认后启动,Okta 备选 → 详见 QUE-661567

### T-005 SAML 2.0 适配 (FEAT-1763F9)
- **负责人**: 待分配 (后端)
- **工期**: 1 天
- **依赖**: T-004
- **状态**: pending
- **验收标准**:
  - 在 IdpProvider 抽象上新增 `SamlProvider` 实现
  - 与 OIDC 共用下游 JWT 签发,业务无感
  - 单测:mock SAML Response,验证 assertion 解析 + 用户映射

### T-006 RBAC 三元组实现 (REQ-565948)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-004
- **状态**: pending
- **验收标准**:
  - PG 表:`roles` / `permissions` / `role_permissions` / `user_roles`
  - 接口 `POST /rbac/check` 三元组 `(role, resource, action)` 鉴权
  - Redis 缓存角色→权限映射,TTL 5min,角色变更主动失效
  - 三元组是 MVP;扩展 ABAC 接口预留,不实现

### T-007 yjs 集成 (FEAT-2F8528)
- **负责人**: 待分配 (前端 + 后端)
- **工期**: 2 天
- **依赖**: T-001
- **状态**: pending
- **验收标准**:
  - 客户端 `yjs@13.6` + `y-websocket` 接入,绑定到富文本编辑器
  - 服务端 `y-websocket` server 模式 + LevelDB persistence(本地测试)
  - 10 人并发同 doc,无丢包/无重复,合并结果一致

### T-008 协同 WebSocket 服务 (FEAT-2F8528)
- **负责人**: 待分配 (后端)
- **工期**: 3 天
- **依赖**: T-007
- **状态**: pending
- **验收标准**:
  - 路径 `WS /collab/{doc_id}`,握手时 JWT 鉴权 + RBAC 校验(T-006)
  - 房间隔离:`doc_id` 为粒度,跨房间不串扰
  - 心跳 30s,90s 无响应踢下线(为飞书 WS 复用,RISK-07A255)
  - 集成测试:多浏览器同时编辑,断线重连后状态同步

### T-009 协同快照持久化 (FEAT-2F8528 / FEAT-BBD14D)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-007, T-008
- **状态**: pending
- **验收标准**:
  - PG 表 `documents(id, doc_state BYTEA, snapshot_at, op_count)`,JSONB 索引 op_count
  - 触发策略:**50 ops 或 5 分钟**(二选一先到)
  - 加载逻辑:启动从 PG 读最新 snapshot + 增量 replay
  - 压测:1000 ops 编辑后崩溃,从快照恢复数据丢失 < 5min 增量

### T-010 Event Bus 基础 (FEAT-C93459)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-001
- **状态**: pending
- **验收标准**:
  - 选 Redis Streams 作 MVP Event Bus,key 形如 `notify:stream:{tenant_id}`
  - 接口 `POST /notify/publish(event_type, payload)`,消费组模式
  - 消费者抽象 `Consumer`,Adapter 注册到 Bus 自动消费

### T-011 飞书 WebSocket Adapter (REQ-0D5DB7, RISK-07A255)
- **负责人**: 待分配 (后端)
- **工期**: 4 天
- **依赖**: T-010
- **状态**: pending
- **验收标准**:
  - 飞书长连接 SDK 接入,事件路由到 `FeishuAdapter.handle(event)`
  - 30s 心跳(RISK-07A255),断线自动重连
  - 重试参数待 QUE-5EE081 拍板,先实现占位(指数退避 + jitter + 死信)
  - 测试:本地 mock 飞书事件,验证消息可靠投递到目标用户

### T-012 钉钉 Stream Adapter (REQ-0D5DB7)
- **负责人**: 待分配 (后端)
- **工期**: 3 天
- **依赖**: T-010
- **状态**: pending
- **验收标准**:
  - 钉钉 Stream 模式 SDK 接入,与飞书共用 Adapter 接口
  - 复用 T-014 重试逻辑
  - MVP 优先级低于飞书,可在 T-019 E2E 后做,但接口必须预留

### T-013 企微回调 Adapter (REQ-0D5DB7)
- **负责人**: 待分配 (后端)
- **工期**: 3 天
- **依赖**: T-010
- **状态**: pending
- **验收标准**:
  - 回调 URL 接收企微事件,验签 + 防重放
  - 与 T-011/T-012 共用 Adapter 接口
  - MVP 优先级最低,接口必须预留

### T-014 指数退避重试 + 死信 (QUE-5EE081)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-010
- **状态**: pending
- **验收标准**:
  - 退避参数:**基数 2s × 2^n, jitter ±20%, 上限 5min**
  - 死信阈值:**重试 5 次后入死信队列**(PG 表 `notify_dlq`)
  - 死信告警:飞书机器人推送(用 T-011)
  - 参数最终值需 V 拍板(QUE-5EE081)

### T-015 Excel 导出核心 (FEAT-BAB8DB / REQ-B12E21)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-001
- **状态**: pending
- **验收标准**:
  - `POST /export/excel` 提交任务,异步 worker 生成 xlsx
  - openpyxl 生成,样式简单(表头加粗,自动列宽)
  - 10 万行数据 < 30s 完成

### T-016 Excel 字段映射配置 (FEAT-BAB8DB / REQ-B12E21)
- **负责人**: 待分配 (后端)
- **工期**: 1 天
- **依赖**: T-015
- **状态**: pending
- **验收标准**:
  - PG 表 `export_field_mappings(tenant_id, template_name, mapping_json)`
  - 模板可热更新,无需重启服务
  - S3 预签名 URL,**24h 过期**,过期清理(T-001 的 S3 生命周期兜底)

### T-017 飞书妙记回调接入 (GOAL-CEDF6C)
- **负责人**: 待分配 (后端)
- **工期**: 2 天
- **依赖**: T-008
- **状态**: pending
- **验收标准**:
  - 飞书妙记 Webhook 接入,事件:会议结束/转写完成
  - 拉取转写文本 + 说话人分段(注意:当前说话人映射是聚类结果,不准)
  - 鉴权:复用 T-004 SSO,妙记回调单独用 HMAC 签名

### T-018 妙记转写写入协同文档 (GOAL-CEDF6C)
- **负责人**: 待分配 (后端 + 前端)
- **工期**: 2 天
- **依赖**: T-007, T-017
- **状态**: pending
- **验收标准**:
  - 转写文本按时间分段写入 yjs doc(分段符 + speaker label)
  - 在线客户端实时看到内容刷入
  - 写入用服务端 `Y.applyUpdate`,不走 WebSocket 广播(避免污染客户端因果链)

### T-019 端到端联调测试 (GOAL-CEDF6C)
- **负责人**: 待分配 (全栈 + QA)
- **工期**: 3 天
- **依赖**: T-006, T-011, T-017, T-018
- **状态**: pending
- **验收标准**:
  - 跑通:**真实会议音视频 → 飞书妙记 → VPBuddy 协同文档可见**
  - 10 人并发在线编辑 + 妙记写入,无冲突
  - 完整流程录屏 + 压测报告(50 种子客户场景模拟)
  - **这是本次端到端测试的硬验收**

---

## 2. 并行/合规 (T-020)

### T-020 微信开放平台账号提前申请 (RISK-2F0662)
- **负责人**: 待分配 (运营)
- **工期**: 1 天 (申请动作) + 7-15 天审核
- **依赖**: 无(必须立即启动,与其他任务并行)
- **状态**: pending
- **验收标准**:
  - 微信开放平台账号提交,企业资质 + 应用场景说明齐全
  - 申请通过前 T-013 走账号密码 fallback 路径
  - 进度同步到飞书项目看板

---

## 3. MVP 后置 (依赖 T-019 E2E 通过)

### T-021 说话人聚类校准 (RISK-F14066)
- **负责人**: 待分配 (算法 + 后端)
- **工期**: 1 天
- **依赖**: T-019
- **状态**: pending
- **验收标准**:
  - 用本次真实会议音频(campplus 当前聚出 8 类,SPEAKER_00/01/02 映射是错的)
  - 校准阈值,期望:3 人会议聚出 3 类(±1)
  - 校准后重新跑 T-019 E2E,验证说话人 label 正确
  - 算法侧 ADR:阈值/参数/嵌入模型选型

### T-022 私有化镜像化 (GOAL-5FB2BA, RISK-9EDDD6)
- **负责人**: 待分配 (SRE)
- **工期**: 2 天
- **依赖**: T-019
- **状态**: pending
- **验收标准**:
  - 单租户镜像:Dockerfile 一份,环境变量注入 PG/Redis/S3 连接串
  - `docker compose up` 一键启动,模型文件走 volume 挂载(避免镜像膨胀)
  - 部署文档:`docs/deploy/private.md`
  - RISK-9EDDD6 模型管理复杂,本期只做"能跑",模型热更新/灰度放后续 ADR

---

## 4. P2 (MVP 后)

### T-023 协同历史回溯 (REQ-1AD367)
- **负责人**: 待分配 (后端)
- **工期**: 3 天
- **依赖**: T-009
- **状态**: pending
- **验收标准**:
  - 基于 T-009 快照 + yjs 自带 history,扩展"时光机" API
  - `GET /collab/{doc_id}/history?at={timestamp}` 返回该时刻 doc 状态
  - 前端:版本树 UI(简化版,只展示快照点)

---

## 5. 待决项追踪

| 待决项 | 影响任务 | 当前倾向 | 阻塞? |
|---|---|---|---|
| QUE-661567 SSO IdP 选型 | T-004 | Azure AD | **是** — V 拍板前 T-004 不启动 |
| QUE-5EE081 飞书 WS 重试参数 | T-011, T-014 | 基数 2s × 2^n + ±20% jitter + 5 次死信 | 否 — 先用占位实现,后续调 |
| RISK-F14066 说话人校准 | T-021 | T-019 跑完后用真实数据校 | 否 — P2 范围 |
| RISK-9EDDD6 私有化模型管理 | T-022 | 单租户镜像化 + ADR 预留 | 否 — 本期只做"能跑" |

---

## 6. YAGNI 备忘(本期不做的)

- ABAC 鉴权(只做 RBAC 三元组)
- 多 IdP 并存(单 IdP 启动,扩展接口预留)
- 协同操作回放 / 注释 / 时光机精确定位(只到快照粒度)
- 钉钉/企微 Stream 协议深度优化(只做 MVP 跑通)
- 模型热更新 / 灰度发布(私有化部署 ADR 细化,本期不做)
- 多租户数据库分片(单 PG 集群够用 50 客户)
- 审计日志全文检索(MVP 只支持按租户+时间范围查)

---

## 7. 变更日志

- 2026-06-21: 首次创建,23 条任务全部 pending。
  - MVP 主线 19 条(T-001 ~ T-019)
  - 并行合规 1 条(T-020)
  - MVP 后置 2 条(T-021 ~ T-022)
  - P2 1 条(T-023)
- 2026-06-21 06:33+: 补丁 — T-004 验收标准补充 RISK-33E783(OAuth 第三方限流)应对措施:对 IdP 调用加本地缓存 + 429 指数退避(复用 T-014 参数),不新增任务,保持 23 条不变。
