# VPBuddy 总体架构文档

- **状态**: v1.43 (2026-07-07 — **百炼 ASR 替换 + 文档自驱动轮询**)
- **关联**: [产品说明书 v2.3](../product-spec/VPBuddy_产品说明书.md) · [API 参考 v0.16.4](../api-reference.md) · [AGENTS.md](../../AGENTS.md)

---

## 〇、口径校准 (v1.20, 2026-06-24)

本架构只服务于一个场景: **我(VP/创业者/产品经理)日常跟各种人开会时，需要能把讨论内容实时沉淀为 6 类结构化物(需求/架构/任务/API/风险/Demo HTML)。沉淀物可以跟同龄人协作讨论，也可以作为下一个会议的上下文。**

### 不是:
- ❌ 不是通用 AI agent 框架
- ❌ 不是会议录播/回放/编解码系统
- ❌ 不是客服聊天机器人
- ❌ 不是企业内部知识管理系统 (但聊天上传可充当)
- ❌ **不是展示项目 / POC / demo** — 是真生产工具

### 用户故事一句话:
> "我开会时开着 VPBuddy 客户端自动收声，会议开始 15 秒后就有 6 类文档 + 1 个 HTML 原型 Demo，我还能跟 AI agent 对话追问细节、调整方向。"

---

## 一、系统概览

```
┌─────────────────────────────┐     ┌──────────────────────────────────────────────┐
│     VP 桌面                   │     │     公网 GPU 服务器 (47.100.182.3)              │
│     (Tauri 2.6+ 客户端)        │     │     vpbuddy-server                             │
│                               │     │                                                │
│  ┌─────────────────────┐      │     │  ┌──────────────────────────────────────────┐  │
│  │ Tauri Rust 后端      │      │     │  │ 百炼 Fun-ASR-Realtime (云端 ASR)           │  │
│  │ (cpal 音频采集)       │──PCM─┼─────┼─▶│ • WebSocket 全双工流                       │  │
│  │ WASAPI/PulseAudio   │ WAV  │     │  │ • 实时逐句转写 → MeetingState.cleaned_text │  │
│  └──────┬──────────────┘      │     │  │ • 模型内部利用上文上下文增强                 │  │
│         │                     │     │  └──────────────────────────────────────────┘  │
│         ▼                     │     │                                                │
│  ┌─────────────────────┐      │     │  ┌──────────────────────────────────────────┐  │
│  │ Vite/JS 前端 (WebView)│     │     │  │ VP Chat 主 agent                           │  │
│  │ - 会议选择/创建       │      │     │  │ session="meeting:{mid}:vp-chat"            │  │
│  │ - 录音控制           │      │     │  └──────────┬───────────────────────────────┘  │
│  │ - 6 文档面板         │      │     │             │ fork (parent_session_id)           │
│  │ - Demo 预览          │      │     │  ┌──────────┴──────────┐                      │
│  │ - Chat 对话          │      │     │  │                      │                      │
│  │ - 协作疑问面板        │      │     │  ▼                      ▼                      │
│  │ - KB 检索/上传        │      │     │ batch_docs agent     demo agent               │
│  └─────────────────────┘      │     │ (5文档/1次LLM调用)   (HTML原型/独立session)      │
│                               │     │                                                │
│  PCM/WS → server              │     │  ┌──────────────────────────────────────────┐  │
│  server SSE ← client          │     │  │ Chroma (RAG/KB)                           │  │
│                               │     │  │ MiniMax-M3 (agent LLM)                    │  │
└─────────────────────────────┘     │  │ task_manager (后台任务队列)                  │  │
                                     │  │ experience_store (经验蒸馏)                 │  │
                                     │  └──────────────────────────────────────────┘  │
                                     └──────────────────────────────────────────────┘
```

### v1.43 关键变更 (2026-07-07)

| 变更 | 旧 (v1.42) | 新 (v1.43) |
|------|-----------|-----------|
| ASR 引擎 | funasr + pyannote (本地 GPU) | **百炼 Fun-ASR-Realtime** (阿里云云端) |
| 音频传输 | 30s WAV chunk HTTP POST | **WebSocket 实时 PCM 流** |
| 转写延迟 | ~28s/chunk (batch) | **逐句实时** (<1s) |
| 上下文增强 | 无 | 百炼模型内部 WS 双工流自动利用 |
| 文档触发 | chunk 处理完或 close | **15s 轮询自驱动** (无需 close), 之后每 30s 增量检查 |
| meeting_id 长度 | 3-32 | **3-48** |

---

## 二、ASR 架构 (v1.43 重写)

### 百炼 Fun-ASR-Realtime

```
客户端 PCM 帧 (16kHz mono int16)
  └─ WebSocket ─→ GPU 服务器 (fastapi_app.py)
                    └─ dashscope Recognition ─→ 百炼云端 WS
                                               │
                                               ▼
                                          sentence_end callback
                                               │
                    ┌──────────────────────────┘
                    ▼
              BailianCallback.on_event()
              ├─ 推送 transcript JSON 回客户端
              └─ 写入 MeetingState.cleaned_text (每句追加)
```

**关键设计**:
1. **单条 WS 连接**: 从 start 到 stop 同一条流，不 restart，模型内部自然利用上文语音特征
2. **句子级写入**: 每句 `sentence_end=True` 时立刻写 `cleaned_text`，文档 agent 实时可见
3. **无外部上下文注入**: dashscope Recognition API 不接受外部 context 参数，也不需要——WS 双工流天然带上下文

---

## 三、文档自驱动轮询 (v1.43 新增)

### 问题

旧架构文档只在 chunk 处理完成或 close 时触发。短会议或实时场景下，用户要等到"会议结束"才能看到文档。

### 方案

```python
# fastapi_app.py — ws_realtime_asr 内
async def _kick_docs():
    await asyncio.sleep(15)  # 等 ASR 积累初始文本
    task_manager.submit(mid, _doc_runner)  # 无条件第一轮
    while doc_running:
        await asyncio.sleep(30)
        if len(cleaned_text) > prev_len + 50:
            task_manager.submit(mid, _doc_runner)  # 文本有增量就重生成
```

**时间线**:
```
T+0s   WebSocket ASR 连接建立
T+1s   开始说话 → 逐句转写 → 写入 cleaned_text
T+15s  第一轮文档生成提交 (task_manager)
T+25s  5/6 文档就绪 (batch_docs)
T+30s  poll 检查 → 文本增量 >50 字 → 第二轮提交
T+45s  demo 通常就绪 (6/6)
...
会议结束 → close → 最后一轮 + 经验蒸馏
```

---

## 四、session 架构 (v1.41, ADR-0041)

```
master session: meeting:{mid}:vp-chat     ← 客户端 Chat 页签
   ├── fork → meeting:{mid}:batch         ← 继承 chat 历史, 生成 5 文档
   └── fork → meeting:{mid}:demo          ← 继承 chat 历史, 生成 demo
```

---

## 五、文档类型

| 文档 | 生成方式 | 内容风格 |
|------|----------|----------|
| req.md | batch_docs (1 次 LLM 调用 5 文档) | 需求分析, bullet points |
| arch.md | batch_docs | 架构方案, 或占位 |
| tasks.md | batch_docs | 任务拆分 |
| api.md | batch_docs | 接口定义, 或占位 |
| risk.md | batch_docs | 风险评估, RISK-XXXXXX 唯一编号 |
| demo HTML | demo agent (独立) | HTML 原型, 可交互 |

所有文档生成通过 **MiniMax-M3** LLM。

---

## 六、数据流 (完整)

```
语音输入 (客户端麦克风)
  → PCM 帧 (16kHz mono int16)
  → WebSocket → 百炼 Fun-ASR-Realtime (云端, 实时逐句)
  → BailianCallback
     ├─ transcript JSON → WS 回客户端 (实时文字)
     └─ MeetingState.cleaned_text += 新句子 (每句追加)
  → 15s 后 → 自驱动 poll 提交 task_manager
     → batch_docs agent (fork 自 chat)
        → AIAgent.chat(prompt) → MiniMax-M3 → write_file × 5
     → demo agent (fork 自 chat)
        → AIAgent.chat(prompt) → MiniMax-M3 → write_file HTML
  → SSE doc-update / demo-new-version 事件
  → 客户端 6 文档面板更新
  → 每 30s → poll 检查增量 → 有增长就重新触发
  → close → 经验蒸馏 + 最终一轮文档
```

---

## 七、后台任务队列 (task_manager)

**设计文档**: [ADR-0042](../decisions/0042-task-manager.md)

per-meeting 单任务队列 + debounce + generation_id + global ThreadPoolExecutor(4)。

---

## 八、经验蒸馏 (experience)

**设计文档**: [ADR-0043](../decisions/0043-experience-distillation.md)

会议结束时从 MeetingState 自动提取 6 类经验候选，JSON 持久化 + 聚合索引。

---

## 九、FastAPI 迁移 (v0.9)

**设计文档**: [ADR-0044](../decisions/0044-fastapi-migration.md)

`fastapi_app.py` 作为主应用，WebSocket 端点 + SSE 事件流 + REST CRUD。旧 `ui_server.py` 保留兼容。

---

## 十、变更历史

| 版本 | 日期 | 变更摘要 | ADR |
|------|------|----------|-----|
| v1.42 | 2026-07-05 | 后台任务队列 + 经验蒸馏 + FastAPI 迁移 + BFF API | ADR-0042~0044 |
| **v1.43** | **2026-07-07** | **百炼 Fun-ASR-Realtime 替换 funasr+pyannote; WebSocket 实时逐句 ASR; 文档自驱动 15s 轮询; meeting_id 3→48** | ADR-0046 |

---

## 十一、参考

- [ADR-0046](../decisions/0046-bailian-asr.md) — 百炼 Fun-ASR-Realtime 接入方案
- [ADR-0042](../decisions/0042-task-manager.md) — 后台任务队列
- [ADR-0044](../decisions/0044-fastapi-migration.md) — FastAPI 迁移
- [API 参考 v0.16.4](../api-reference.md)
